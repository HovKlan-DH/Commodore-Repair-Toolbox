<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CRT"
             xmlns:ts="clr-namespace:Tabs.TabSchematics"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="CRT.TabSchematics">

	<Grid x:Name="SchematicsInnerGrid">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" MinWidth="80" />
			<ColumnDefinition Width="4" />
			<ColumnDefinition Width="*" MinWidth="100" />
		</Grid.ColumnDefinitions>

		<Border Grid.Column="0"
			x:Name="SchematicsContainer"
			Focusable="True"
			ClipToBounds="True"
			PointerWheelChanged="OnSchematicsZoom"
			PointerPressed="OnSchematicsPointerPressed"
			PointerMoved="OnSchematicsPointerMoved"
			PointerReleased="OnSchematicsPointerReleased"
			Padding="0,0,0,0">
			<Grid>
				<TextBlock x:Name="SchematicsMissingImageText"
                           Text="Missing image - please refresh board after sync finishes..."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Fg}"
                           IsVisible="False" />

				<Image x:Name="SchematicsImage"
                       Stretch="Uniform"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       RenderTransformOrigin="0%,0%"
                       RenderOptions.BitmapInterpolationMode="HighQuality">
					<Image.RenderTransform>
						<MatrixTransform />
					</Image.RenderTransform>
				</Image>

				<ts:SchematicHighlightsOverlay x:Name="SchematicsHighlightsOverlay"
	  IsHitTestVisible="False"
	  RenderTransformOrigin="0%,0%">
					<ts:SchematicHighlightsOverlay.RenderTransform>
						<MatrixTransform />
					</ts:SchematicHighlightsOverlay.RenderTransform>
				</ts:SchematicHighlightsOverlay>

				<ts:SchematicHighlightsOverlay x:Name="SchematicsHoverHighlightsOverlay"
					  IsHitTestVisible="False"
					  RenderTransformOrigin="0%,0%">
					<ts:SchematicHighlightsOverlay.RenderTransform>
						<MatrixTransform />
					</ts:SchematicHighlightsOverlay.RenderTransform>
				</ts:SchematicHighlightsOverlay>

				<Canvas x:Name="SchematicsPolylineCanvas"
								IsHitTestVisible="False"
								RenderTransformOrigin="0%,0%"
								Background="Transparent">
					<Canvas.RenderTransform>
						<MatrixTransform />
					</Canvas.RenderTransform>
				</Canvas>

				<ts:ComponentLabelEditorOverlay x:Name="SchematicsLabelEditorOverlay"
												IsVisible="False"
												IsHitTestVisible="False"
												RenderTransformOrigin="0%,0%">
					<ts:ComponentLabelEditorOverlay.RenderTransform>
						<MatrixTransform />
					</ts:ComponentLabelEditorOverlay.RenderTransform>
				</ts:ComponentLabelEditorOverlay>

				<Canvas x:Name="SchematicsLabelsCanvas"
						IsHitTestVisible="False"
						RenderTransformOrigin="0%,0%"
						Background="Transparent"
						ZIndex="100">
					<Canvas.RenderTransform>
						<MatrixTransform />
					</Canvas.RenderTransform>
				</Canvas>

				<ts:KiCadOverlayRenderControl x:Name="SchematicsKiCadOverlayCanvas"
							  IsHitTestVisible="False"
							  RenderTransformOrigin="0%,0%">
					<ts:KiCadOverlayRenderControl.RenderTransform>
						<MatrixTransform />
					</ts:KiCadOverlayRenderControl.RenderTransform>
				</ts:KiCadOverlayRenderControl>

				<StackPanel Orientation="Horizontal"
							HorizontalAlignment="Left"
							VerticalAlignment="Top"
							Margin="10,10,0,0"
							Spacing="8"
							IsHitTestVisible="False">

					<Border x:Name="SchematicsNameBorder"
							IsVisible="False"
							Background="{DynamicResource Schematics_Name_Bg}"
							BorderBrush="{DynamicResource Schematics_Name_Border}"
							BorderThickness="1"
							CornerRadius="4"
							Padding="8,4">
						<TextBlock x:Name="SchematicsNameLabel"
						   FontSize="11"
						   FontWeight="Normal"
						   Foreground="{DynamicResource Schematics_Name_Fg}" />
					</Border>

					<Border x:Name="SchematicsRegionBorder"
						IsVisible="False"
						BorderThickness="1"
						CornerRadius="4"
						Padding="8,4">
						<TextBlock x:Name="SchematicsRegionLabel"
					   FontSize="11"
					   FontWeight="Normal" />
					</Border>
				</StackPanel>

				<StackPanel Orientation="Horizontal"
					HorizontalAlignment="Left"
					VerticalAlignment="Top"
					Margin="10,44,0,0"
					Spacing="6"
					IsHitTestVisible="False">
					<Border x:Name="SchematicsHoverLabelBorder"
						IsVisible="False"
						Background="{DynamicResource Schematics_ComponentHover_Bg}"
						BorderBrush="{DynamicResource Schematics_ComponentHover_Border}"
						BorderThickness="1"
						CornerRadius="4"
						Padding="8,4">
						<TextBlock x:Name="SchematicsHoverLabelText"
							FontSize="11"
									FontWeight="Bold"
									Foreground="{DynamicResource Schematics_ComponentHover_Fg}" />
					</Border>
					<Border x:Name="SchematicsHoverPadBorder"
						IsVisible="False"
						Background="{DynamicResource Schematics_ComponentHover_Bg}"
						BorderBrush="{DynamicResource Schematics_ComponentHover_Border}"
						BorderThickness="1"
						CornerRadius="4"
						Padding="8,4">
						<StackPanel Orientation="Horizontal" Spacing="2">
							<TextBlock Text="Pin"
								FontSize="11"
								FontWeight="Normal"
								Foreground="{DynamicResource Schematics_ComponentHover_Fg}" />
							<TextBlock x:Name="SchematicsHoverPadText"
								FontSize="11"
								FontWeight="Bold"
								Foreground="{DynamicResource Schematics_ComponentHover_Fg}" />
						</StackPanel>
					</Border>
				</StackPanel>

				<!-- BOTTOM LEFT SETTINGS PANELS -->
				<Grid HorizontalAlignment="Left"
					  VerticalAlignment="Bottom"
					  Margin="10,0,0,10"
					  RowDefinitions="Auto"
					  ColumnDefinitions="Auto,Auto,Auto"
					  ColumnSpacing="8"
					  ZIndex="200">

					<!-- GLOBAL SETTINGS PANEL -->
					<Border x:Name="GlobalSettingsPanel"
							Grid.Row="0"
							Grid.Column="0"
							HorizontalAlignment="Left"
							VerticalAlignment="Bottom"
							Background="{DynamicResource Schematics_Panels_Bg}"
							BorderBrush="{DynamicResource Schematics_Panels_Border}"
							BorderThickness="1"
							CornerRadius="4"
							Padding="8,8">
						<StackPanel Spacing="6">

							<Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
								<TextBlock Grid.Column="0"
										   Text="Global settings"
										   FontSize="11"
										   VerticalAlignment="Center"
										   FontWeight="Bold"
										   Foreground="{DynamicResource Schematics_Panels_Fg}" />
								<Button x:Name="ToggleGlobalSettingsPanelButton"
										Grid.Column="1"
										Content="Collapse"
										FontSize="9"
										Padding="6,2"
										HorizontalAlignment="Right"
										VerticalAlignment="Top" />
							</Grid>

							<StackPanel x:Name="GlobalSettingsListPanel" Spacing="4">

								<StackPanel x:Name="GlobalHoverHighlightsTracesRow"
											Orientation="Horizontal"
											Spacing="6"
											Background="Transparent"
											Cursor="Hand"
											PointerPressed="OnGlobalHoverHighlightsTracesRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckGlobalHoverHighlightsTraces"
												  MinHeight="0"
												  Margin="0"
												  Padding="0"
												  CornerRadius="2"
												  IsChecked="True"
												  IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Highlight trace on hover"
											   FontSize="11"
											   Foreground="{DynamicResource SchematicsPanels_Fg}"
											   VerticalAlignment="Center"
											   IsHitTestVisible="False" />
								</StackPanel>

								<StackPanel x:Name="SchematicsInteractiveCadTraceHoverHoldShiftRow"
										IsVisible="False"
										Orientation="Horizontal"
										Spacing="6"
										Background="Transparent"
										Cursor="Hand"
										PointerPressed="OnSchematicsInteractiveCadTraceHoverHoldShiftRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="SchematicsInteractiveCadTraceHoverHoldShiftCheckBox"
												  MinHeight="0"
												  Margin="0"
												  Padding="0"
												  CornerRadius="2"
												  VerticalAlignment="Center"
												  IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Hold SHIFT to highlight trace on hover"
											   FontSize="11"
											   Foreground="{DynamicResource SchematicsPanels_Fg}"
											   VerticalAlignment="Center"
											   IsHitTestVisible="False" />
								</StackPanel>

								<StackPanel x:Name="GlobalShowTracesOnComponentSelectRow"
									Orientation="Horizontal"
									Spacing="6"
									Background="Transparent"
									Cursor="Hand"
									PointerPressed="OnGlobalShowTracesOnComponentSelectRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckGlobalShowTracesOnComponentSelect"
												  MinHeight="0"
												  Margin="0"
												  Padding="0"
												  CornerRadius="2"
												  IsChecked="True"
												  IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Highlight component traces on select"
											   FontSize="11"
											   Foreground="{DynamicResource SchematicsPanels_Fg}"
											   VerticalAlignment="Center"
											   IsHitTestVisible="False" />
								</StackPanel>

								<StackPanel x:Name="BoardShowTracesOnSelectedComponentRow"
											Orientation="Horizontal"
											Spacing="6"
											Background="Transparent"
											Cursor="Hand"
											PointerPressed="OnBoardShowTracesOnSelectedComponentRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckBoardShowTracesOnSelectedComponent"
												  MinHeight="0"
												  Margin="0"
												  Padding="0"
												  CornerRadius="2"
												  IsChecked="True"
												  IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Highlight component traces on hover"
											   FontSize="11"
											   Foreground="{DynamicResource SchematicsPanels_Fg}"
											   VerticalAlignment="Center"
											   IsHitTestVisible="False" />
								</StackPanel>
								
								<StackPanel x:Name="GlobalShowOppositeSideTracesRow"
											IsVisible="False"
											Orientation="Horizontal"
											Spacing="6"
											Background="Transparent"
											Cursor="Hand"
											PointerPressed="OnGlobalShowOppositeSideTracesRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckGlobalShowOppositeSideTraces"
												  MinHeight="0"
												  Margin="0"
												  Padding="0"
												  CornerRadius="2"
												  IsChecked="False"
												  IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Show traces from opposite side"
											   FontSize="11"
											   Foreground="{DynamicResource SchematicsPanels_Fg}"
											   VerticalAlignment="Center"
											   IsHitTestVisible="False" />
								</StackPanel>

								<StackPanel x:Name="GlobalShowCalibrationTracesAndPadsRow"
										IsVisible="False"
										Orientation="Horizontal"
										Spacing="6"
										Background="Transparent"
										Cursor="Hand"
										PointerPressed="OnGlobalShowCalibrationTracesAndPadsRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckGlobalShowCalibrationTracesAndPads"
												  MinHeight="0"
												  Margin="0"
												  Padding="0"
												  CornerRadius="2"
												  IsChecked="True"
												  IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Show traces and pads"
											   FontSize="11"
											   FontWeight="Bold"
											   Foreground="{DynamicResource Text_Fail_Fg}"
											   VerticalAlignment="Center"
											   IsHitTestVisible="False" />
								</StackPanel>
								
								<StackPanel x:Name="GlobalShowZonesRow"
											IsVisible="False"
											Orientation="Horizontal"
											Spacing="6"
											Background="Transparent"
											Cursor="Hand"
											PointerPressed="OnGlobalShowZonesRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckGlobalShowZones"
												  MinHeight="0"
												  Margin="0"
												  Padding="0"
												  CornerRadius="2"
												  IsChecked="True"
												  IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Show zones"
											   FontSize="11"
											   Foreground="{DynamicResource SchematicsPanels_Fg}"
											   VerticalAlignment="Center"
											   IsHitTestVisible="False" />
								</StackPanel>

								<StackPanel x:Name="BoardMarkPin1OnSelectedComponentRow"
									IsVisible="False"
									Orientation="Horizontal"
									Spacing="6"
									Background="Transparent"
									Cursor="Hand"
									PointerPressed="OnBoardMarkPin1OnSelectedComponentRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckBoardMarkPin1OnSelectedComponent"
												  MinHeight="0"
												  Margin="0"
												  Padding="0"
												  CornerRadius="2"
												  IsChecked="False"
												  IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Mark first pin on component"
											   FontSize="11"
											   Foreground="{DynamicResource SchematicsPanels_Fg}"
											   VerticalAlignment="Center"
											   IsHitTestVisible="False"/>
								</StackPanel>

								<StackPanel Orientation="Horizontal" Spacing="6" Background="Transparent" Cursor="Hand" PointerPressed="OnBoardContributorModeRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckBoardContributorMode" MinHeight="0" Margin="0" Padding="0" CornerRadius="2" IsChecked="False" IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Enable contributor mode" FontSize="11" Foreground="{DynamicResource SchematicsPanels_Fg}" VerticalAlignment="Center" IsHitTestVisible="False"/>
								</StackPanel>

							</StackPanel>
						</StackPanel>
					</Border>

					<!-- LABELS VISIBLE PANEL -->
					<Border x:Name="LabelsPanel"
							Grid.Row="0"
							Grid.Column="1"
							HorizontalAlignment="Left"
							VerticalAlignment="Bottom"
							Background="{DynamicResource Schematics_Panels_Bg}"
							BorderBrush="{DynamicResource Schematics_Panels_Border}"
							BorderThickness="1"
							CornerRadius="4"
							Padding="8,8">
						<StackPanel Spacing="6">
							<Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
								<TextBlock Grid.Column="0"
										   Text="Labels visible"
										   FontSize="11"
										   VerticalAlignment="Center"
										   FontWeight="Bold"
										   Foreground="{DynamicResource Schematics_Panels_Fg}" />
								<Button x:Name="ToggleLabelsPanelButton"
										Grid.Column="1"
										Content="Collapse"
										FontSize="9"
										Padding="6,2"
										HorizontalAlignment="Right"
										VerticalAlignment="Top" />
							</Grid>

							<StackPanel x:Name="LabelsListPanel" Spacing="4">
								<StackPanel Orientation="Horizontal" Spacing="6" Background="Transparent" Cursor="Hand" PointerPressed="OnLabelBoardRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckLabelBoard" MinHeight="0" Margin="0" Padding="0" CornerRadius="2" IsChecked="False" IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Board label" FontSize="11" Foreground="{DynamicResource SchematicsPanels_Fg}" VerticalAlignment="Center" IsHitTestVisible="False"/>
								</StackPanel>

								<StackPanel Orientation="Horizontal" Spacing="6" Background="Transparent" Cursor="Hand" PointerPressed="OnLabelTechnicalRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckLabelTechnical" MinHeight="0" Margin="0" Padding="0" CornerRadius="2" IsChecked="False" IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Technical name" FontSize="11" Foreground="{DynamicResource SchematicsPanels_Fg}" VerticalAlignment="Center" IsHitTestVisible="False"/>
								</StackPanel>

								<StackPanel Orientation="Horizontal" Spacing="6" Background="Transparent" Cursor="Hand" PointerPressed="OnLabelFriendlyRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckLabelFriendly" MinHeight="0" Margin="0" Padding="0" CornerRadius="2" IsChecked="False" IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Friendly name" FontSize="11" Foreground="{DynamicResource SchematicsPanels_Fg}" VerticalAlignment="Center" IsHitTestVisible="False"/>
								</StackPanel>

								<StackPanel Orientation="Horizontal" Spacing="6" Background="Transparent" Cursor="Hand" PointerPressed="OnLabelSelectedOnlyRowClicked">
									<Viewbox Width="20" Height="20" IsHitTestVisible="False">
										<CheckBox x:Name="CheckLabelSelectedOnly" MinHeight="0" Margin="0" Padding="0" CornerRadius="2" IsChecked="False" IsHitTestVisible="False" />
									</Viewbox>
									<TextBlock Text="Selected components only" FontSize="11" Foreground="{DynamicResource SchematicsPanels_Fg}" VerticalAlignment="Center" IsHitTestVisible="False"/>
								</StackPanel>
							</StackPanel>
						</StackPanel>
					</Border>

					<!-- IMPORTANT SIGNALS PANEL -->
					<Border x:Name="ImportantSignalsPanel"
							Grid.Row="0"
							Grid.Column="2"
							HorizontalAlignment="Left"
							VerticalAlignment="Bottom"
							Background="{DynamicResource Schematics_Panels_Bg}"
							BorderBrush="{DynamicResource Schematics_Panels_Border}"
							BorderThickness="1"
							CornerRadius="4"
							Padding="8,8"
							IsVisible="False">
						<StackPanel Spacing="6">
							<Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
								<TextBlock x:Name="ImportantSignalsHeaderTextBlock"
									   Grid.Column="0"
									   Text="Important signals"
									   FontSize="11"
									   VerticalAlignment="Center"
									   FontWeight="Bold"
									   Foreground="{DynamicResource Schematics_Panels_Fg}" />
								<Button x:Name="ToggleImportantSignalsPanelButton"
										Grid.Column="1"
										Content="Collapse"
										FontSize="9"
										Padding="6,2"
										HorizontalAlignment="Right"
										VerticalAlignment="Top" />
							</Grid>

							<StackPanel x:Name="ImportantSignalsListPanel" Spacing="6">
								<ListBox x:Name="ImportantSignalsListBox"
										 SelectionMode="Multiple,Toggle"
										 MinWidth="180"
										 MaxHeight="180"
										 BorderThickness="1"
										 CornerRadius="4"
										 ClipToBounds="True"
										 Padding="0"
										 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
									<ListBox.ItemTemplate>
										<DataTemplate>
											<TextBlock Text="{ReflectionBinding DisplayName}"
													   ToolTip.Tip="{ReflectionBinding ToolTipText}"
													   TextWrapping="Wrap" />
										</DataTemplate>
									</ListBox.ItemTemplate>

									<ListBox.Styles>
										<Style Selector="ListBox /template/ Border">
											<Setter Property="CornerRadius" Value="3" />
											<Setter Property="ClipToBounds" Value="True" />
										</Style>
										<Style Selector="ListBox /template/ ScrollViewer">
											<Setter Property="CornerRadius" Value="3" />
											<Setter Property="ClipToBounds" Value="True" />
										</Style>
										<Style Selector="ListBoxItem">
											<Setter Property="Padding" Value="8,3" />
											<Setter Property="MinHeight" Value="0" />
											<Setter Property="FontSize" Value="11" />
										</Style>
									</ListBox.Styles>
								</ListBox>
							</StackPanel>

							<Button x:Name="ClearImportantSignalsButton"
								Content="Clear All"
								FontSize="9"
								Padding="6,2"
								Margin="0,8,0,0"
								HorizontalAlignment="Stretch"
								HorizontalContentAlignment="Center"
								IsVisible="True"
								IsEnabled="False" />
						</StackPanel>
					</Border>
				</Grid>

				<!-- DYNAMIC POLYLINE TRACES LIST PANEL -->
				<Border x:Name="TracesPanel"
						IsVisible="False"
						HorizontalAlignment="Right"
						VerticalAlignment="Bottom"
						Margin="0,0,10,10"
						ZIndex="200"
						Background="{DynamicResource Schematics_Panels_Bg}"
						BorderBrush="{DynamicResource Schematics_Panels_Border}"
						BorderThickness="1"
						CornerRadius="4"
						Padding="8,8">
					<StackPanel Spacing="6">
						<StackPanel Orientation="Horizontal" Spacing="6">
							<TextBlock Text="Traces visible"
									   FontSize="11"
									   VerticalAlignment="Center"
									   FontWeight="Bold"
									   Foreground="{DynamicResource SchematicsPanels_Fg}" />
							<Button x:Name="ClearTracesButton"
									Content="Clear All"
									FontSize="9"
									Padding="6,2"
									VerticalAlignment="Center" />
						</StackPanel>
						<StackPanel x:Name="TracesListPanel" Spacing="4" />

						<Button x:Name="UndoDeletedTraceButton"
								Content="Undo deletion"
								FontSize="9"
								Padding="6,2"
								HorizontalAlignment="Stretch"
								HorizontalContentAlignment="Center"
								IsVisible="False" />
					</StackPanel>
				</Border>

				<!-- CONTEXTUAL FLOATING TRACE PALETTE -->
				<Border x:Name="TraceFloatingPalette"
						IsVisible="False"
						HorizontalAlignment="Left"
						VerticalAlignment="Top"
						ZIndex="300"
						Background="{DynamicResource Schematics_Panels_Bg}"
						BorderBrush="{DynamicResource Schematics_Panels_Border}"
						BorderThickness="1"
						CornerRadius="8"
						Padding="8,6"
						BoxShadow="0 4 10 0 #40000000">
					<StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">

						<StackPanel x:Name="DynamicPaletteColorsPanel" Orientation="Horizontal" Spacing="10" />

						<Border Width="1" Height="18" Background="{DynamicResource Schematics_Panels_Fg}" Margin="2,0"/>

						<Button x:Name="CustomColorButton"
								Width="18" Height="18"
								CornerRadius="9"
								Padding="0"
								Cursor="Hand"
								Background="White"
								BorderBrush="White"
								BorderThickness="1">
							<Button.Flyout>
								<Flyout Placement="BottomEdgeAlignedLeft">
									<ColorView x:Name="TraceColorPicker"
											   Color="White"
											   PropertyChanged="OnTraceColorPickerPropertyChanged" />
								</Flyout>
							</Button.Flyout>
						</Button>

						<Border Width="1" Height="18" Background="{DynamicResource SchematicsPanels_Fg}" Margin="2,0"/>

						<TextBlock Text="🗑"
								   FontSize="16"
								   Cursor="Hand"
								   PointerPressed="OnPaletteDeleteClicked"
								   VerticalAlignment="Center"
								   Margin="2,0"/>
					</StackPanel>
				</Border>

				<!-- KICAD NET CONNECTIONS PANEL -->
				<Border x:Name="KiCadNetConnectionsPanel"
						IsVisible="False"
						HorizontalAlignment="Right"
						VerticalAlignment="Top"
						Margin="10,10,10,10"
						ZIndex="200"
						Background="{DynamicResource Schematics_Panels_Bg}"
						BorderBrush="{DynamicResource Schematics_Panels_Border}"
						BorderThickness="1"
						CornerRadius="4"
						Padding="8,8"
						MinWidth="120"
						MaxWidth="250">

					<StackPanel Spacing="6">
						<Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
							<TextBlock x:Name="KiCadNetConnectionsHeaderTextBlock"
								   Grid.Column="0"
								   Text="Netlist names"
								   FontSize="11"
								   VerticalAlignment="Center"
								   FontWeight="Bold"
								   Foreground="{DynamicResource Schematics_Panels_Fg}" />
							<Button x:Name="ToggleKiCadNetConnectionsPanelButton"
									Grid.Column="1"
									Content="Collapse"
									FontSize="9"
									Padding="6,2"
									HorizontalAlignment="Right"
									VerticalAlignment="Top" />
						</Grid>

						<StackPanel x:Name="KiCadNetConnectionsContentPanel" Spacing="4">
							<TextBlock x:Name="KiCadNetConnectionsNetNameText"
									   FontSize="11"
									   Foreground="{DynamicResource Schematics_Panels_Fg}"
									   TextWrapping="Wrap"
									   Margin="8,0,0,4" />

							<TextBlock Text="Connected to:"
									   FontSize="11"
									   FontWeight="Bold"
									   Foreground="{DynamicResource Schematics_Panels_Fg}" />

							<ScrollViewer HorizontalScrollBarVisibility="Disabled"
										  Margin="0,4,0,0"
										  MaxHeight="260">
								<ItemsControl x:Name="KiCadNetConnectionsList">
									<ItemsControl.ItemTemplate>
										<DataTemplate>
											<TextBlock Text="{Binding}"
													   FontSize="11"
													   Foreground="{DynamicResource Schematics_Panels_Fg}"
													   Margin="8,2,0,0"/>
										</DataTemplate>
									</ItemsControl.ItemTemplate>
								</ItemsControl>
							</ScrollViewer>
						</StackPanel>

						<Button x:Name="ClearKiCadTraceSelectionButton"
								Content="Clear All"
								FontSize="9"
								Padding="6,2"
								Margin="0,8,0,0"
								HorizontalAlignment="Stretch"
								HorizontalContentAlignment="Center"
								IsVisible="False"
								IsEnabled="False" />
					</StackPanel>
				</Border>

			</Grid>
		</Border>

		<GridSplitter x:Name="SchematicsSplitter"
                      Grid.Column="1"
                      ResizeDirection="Columns"
                      HorizontalAlignment="Stretch" />

		<ListBox x:Name="SchematicsThumbnailList"
                 Grid.Column="2"
                 Background="Transparent"
                 SelectionMode="Single"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

			<ListBox.Styles>
				<Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
					<Setter Property="Background" Value="Transparent" />
				</Style>
				<Style Selector="ListBoxItem:selected /template/ ContentPresenter">
					<Setter Property="Background" Value="Transparent" />
				</Style>

				<Style Selector="ListBoxItem:pointerover">
					<Setter Property="Cursor" Value="Hand" />
				</Style>

				<Style Selector="ListBoxItem">
					<Setter Property="Padding"                     Value="0" />
					<Setter Property="HorizontalContentAlignment" Value="Stretch" />
				</Style>

				<Style Selector="ListBoxItem Border.ThumbnailBorder">
					<Setter
                        Property="BorderBrush"
                        Value="{DynamicResource Thumbnail_Border}" />
					<Setter
                        Property="BorderThickness"
                        Value="1" />
				</Style>

				<Style Selector="ListBoxItem:selected Border.ThumbnailBorder">
					<Setter
                        Property="BorderBrush"
                        Value="{DynamicResource Thumbnail_BorderSelected}" />
					<Setter
                        Property="BorderThickness"
                        Value="3" />
				</Style>

			</ListBox.Styles>
			<ListBox.ItemTemplate>
				<DataTemplate x:DataType="ts:SchematicThumbnail">
					<Border Classes="ThumbnailBorder"
                            Margin="4,4,8,4"
                            CornerRadius="0"
                            HorizontalAlignment="Stretch"
                            ClipToBounds="True"
                            Opacity="{Binding VisualOpacity}"
                            DragDrop.AllowDrop="True"
                            PointerPressed="OnThumbnailPointerPressed"
                            PointerMoved="OnThumbnailPointerMoved"
                            PointerReleased="OnThumbnailPointerReleased">
						<Grid>
							<Grid IsVisible="{Binding IsDropPlaceholder}"
								  Height="{Binding PlaceholderHeight}"
								  Width="{Binding PlaceholderWidth}"
								  HorizontalAlignment="Left">
								<Border BorderBrush="{DynamicResource Thumbnail_BorderSelected}"
										BorderThickness="2"
										Background="#10FF0000" />
							</Grid>
							<Grid IsVisible="{Binding !IsDropPlaceholder}">
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto" />
									<RowDefinition Height="*" />
								</Grid.RowDefinitions>

								<Border Grid.Row="0"
									Background="{DynamicResource Thumbnail_Bg}"
									BorderBrush="{DynamicResource Thumbnail_Border}"
									BorderThickness="0,0,0,1">
									<Grid>
										<Border Background="{DynamicResource Thumbnail_Selection_Bg}"
												Opacity="1.0"
												IsVisible="{Binding IsMatchForSelection}" />
										<TextBlock Text="{Binding Name}"
												   Foreground="{DynamicResource Thumbnail_Fg}"
												   HorizontalAlignment="Stretch"
												   TextAlignment="Left"
												   FontSize="11"
												   Padding="4,3"
												   IsVisible="{Binding !IsMatchForSelection}" />
										<TextBlock Text="{Binding Name}"
												   Foreground="{DynamicResource Thumbnail_Selection_Fg}"
												   HorizontalAlignment="Stretch"
												   TextAlignment="Left"
												   FontSize="11"
												   Padding="4,3"
												   IsVisible="{Binding IsMatchForSelection}" />
									</Grid>
								</Border>

								<Grid Grid.Row="1">
									<TextBlock Text="Missing Image"
											   HorizontalAlignment="Center"
											   VerticalAlignment="Center"
											   Foreground="{DynamicResource Fg}"
											   FontSize="11"
											   Padding="8,16"
											   TextAlignment="Center"
											   TextWrapping="Wrap"
											   IsVisible="{Binding ImageSource, Converter={x:Static ObjectConverters.IsNull}}" />

									<Image Source="{Binding ImageSource}"
										   Stretch="Uniform"
										   HorizontalAlignment="Stretch"
										   RenderOptions.BitmapInterpolationMode="HighQuality" />
								</Grid>
							</Grid>
						</Grid>
					</Border>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<Border x:Name="ThumbnailDragGhost"
				Grid.Column="2"
				IsVisible="False"
				IsHitTestVisible="False"
				HorizontalAlignment="Left"
				VerticalAlignment="Top"
				Width="160"
				Opacity="0.92"
				ZIndex="1000">
			<Border.RenderTransform>
				<TranslateTransform />
			</Border.RenderTransform>

			<Border.Background>
				<SolidColorBrush Color="#F2FFFFFF" />
			</Border.Background>

			<Border.BorderBrush>
				<SolidColorBrush Color="#CCB00000" />
			</Border.BorderBrush>

			<Border.BorderThickness>2</Border.BorderThickness>
			<Border.CornerRadius>2</Border.CornerRadius>

			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>

				<Border Grid.Row="0"
						Background="{DynamicResource Thumbnail_Bg}"
						BorderBrush="{DynamicResource Thumbnail_Border}"
						BorderThickness="0,0,0,1">
					<TextBlock x:Name="ThumbnailDragGhostName"
							   FontSize="11"
							   Padding="4,3"
							   HorizontalAlignment="Stretch"
							   TextAlignment="Left"
							   Foreground="{DynamicResource Thumbnail_Fg}" />
				</Border>

				<Image x:Name="ThumbnailDragGhostImage"
					   Grid.Row="1"
					   Stretch="Uniform"
					   HorizontalAlignment="Stretch"
					   RenderOptions.BitmapInterpolationMode="HighQuality" />
			</Grid>
		</Border>

		<Border x:Name="SchematicsLabelEditorMenuBorder"
				IsVisible="False"
				ZIndex="5000"
				HorizontalAlignment="Left"
				VerticalAlignment="Top"
				Background="{DynamicResource Schematics_Panels_Bg}"
				BorderBrush="{DynamicResource Schematics_Panels_Border}"
				BorderThickness="1"
				CornerRadius="6"
				Padding="10,8">
			<StackPanel x:Name="SchematicsLabelEditorMenuPanel"
						Spacing="8"
						Width="235">
				<TextBlock x:Name="SchematicsLabelEditorMenuStateTextBlock"
						   Text="Contributor mode actions"
						   FontSize="11"
						   FontWeight="Bold"
						   Foreground="{DynamicResource Schematics_Panels_Fg}" />

				<Button x:Name="EnableLabelEditorButton"
						Content="Enable component label editor"
						HorizontalAlignment="Stretch" />

				<Button x:Name="ApplyLabelEditorChangesButton"
					Content="Apply all editor changes"
					IsVisible="False"
					HorizontalAlignment="Stretch"
					Foreground="{DynamicResource Button_Ok_Fg}"
					Background="{DynamicResource Button_Ok_Bg}"
					BorderBrush="{DynamicResource Button_Ok_Border}" />
				
				<Button x:Name="CancelLabelEditorChangesButton"
						Content="Discard all editor changes"
						IsVisible="False"
						HorizontalAlignment="Stretch"
						Foreground="{DynamicResource Button_Cancel_Fg}"
						Background="{DynamicResource Button_Cancel_Bg}"
						BorderBrush="{DynamicResource Button_Cancel_Border}" />

				<Button x:Name="BeginKiCadTraceCalibrationButton"
						Content="Calibrate KiCad traces"
						HorizontalAlignment="Stretch" />

				<Button x:Name="ApplyKiCadTraceCalibrationButton"
						Content="Apply KiCad calibration"
						IsVisible="False"
						HorizontalAlignment="Stretch"
						Foreground="{DynamicResource Button_Ok_Fg}"
						Background="{DynamicResource Button_Ok_Bg}"
						BorderBrush="{DynamicResource Button_Ok_Border}" />

				<Button x:Name="CancelKiCadTraceCalibrationButton"
						Content="Discard KiCad calibration"
						IsVisible="False"
						HorizontalAlignment="Stretch"
						Foreground="{DynamicResource Button_Cancel_Fg}"
						Background="{DynamicResource Button_Cancel_Bg}"
						BorderBrush="{DynamicResource Button_Cancel_Border}" />

				
			</StackPanel>
		</Border>

		<Border x:Name="SchematicsNewLabelPromptBorder"
				IsVisible="False"
				ZIndex="5001"
				HorizontalAlignment="Left"
				VerticalAlignment="Top"
				Background="{DynamicResource Schematics_Panels_Bg}"
				BorderBrush="{DynamicResource Schematics_Panels_Border}"
				BorderThickness="1"
				CornerRadius="6"
				Padding="10,8">
			<StackPanel Spacing="8"
						Width="260">
				<TextBlock Text="New component label"
						   FontSize="11"
						   FontWeight="Bold"
						   Foreground="{DynamicResource Schematics_Panels_Fg}" />

				<TextBlock Text="Board label"
						   FontSize="11"
						   Foreground="{DynamicResource Schematics_Panels_Fg}" />

				<TextBox x:Name="NewLabelEditorBoardLabelTextBox" />

				<TextBlock Text="Category"
						   FontSize="11"
						   Foreground="{DynamicResource Schematics_Panels_Fg}" />

				<ComboBox x:Name="NewLabelEditorCategoryComboBox"
						  HorizontalAlignment="Stretch" />

				<StackPanel Orientation="Horizontal"
							Spacing="8"
							HorizontalAlignment="Right">
					<Button x:Name="CancelNewLabelEditorBoardLabelButton"
							Content="Cancel"
							MinWidth="72" />
					<Button x:Name="ConfirmNewLabelEditorBoardLabelButton"
							Content="OK"
							MinWidth="72" />
				</StackPanel>
			</StackPanel>
		</Border>

	</Grid>
</UserControl>